const styles = (theme) => {
  return {
    container: {
      display: "flex",
      flexDirection: "column",
    },
    main: {
      flexGrow: "1",
    },
  };
};

export default styles;
